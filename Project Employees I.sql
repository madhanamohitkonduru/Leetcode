select t.project_id, round(avg(t.experience_years),2) as average_years from (select p.project_id, e.employee_id, e.experience_years from Project p left join Employee e on p.employee_id = e.employee_id) t group by t.project_id